
@{
    ViewBag.Title = "Consent Form";
    //Layout = "~/Views/_ViewStart.cshtml";
}
 


<div class="modal fade" id="myModalEdit" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" id="modalEditPopup">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" id="btnClose" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="gridSystemModalLabel">Edit Client Details</h4>
            </div>
            <div class="modal-body row" id="EditClientDiv">
            </div>
        </div>
    </div>
</div>


<!-- LIST OF CLIENT START-->
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Consent Form List</h5>                    
                </div>
                <div class="ibox-content">
                    @*<div class="row">
                        <div class="form-group col-md-2 pull-right">
                            <label>Office:</label>

                            @Html.DropDownList("ddFilterOffice", (System.Web.Mvc.SelectList)ViewBag.lstOffice, "All", new { @class = "form-control", onchange = "Filtration();" })
                        </div>
                    </div>*@
                    @*<input type="hidden" id="hdnPatientRequestId" />*@
                    <table id="myDataTable" class="table table-striped table-bordered table-hover dataTables-example">
                        <thead>
                            <tr>
                                <th class="hidden"><strong>AdmissionId</strong></th>
                                <th><strong>PatientName</strong></th>
                                <th><strong>CaregiverName</strong></th>                               
                                <th><strong>Office</strong></th>
                                <th><strong>MRN Number</strong></th>
                                <th><strong>Date of Submit</strong></th>
                                <th><strong>Admit Date</strong></th>
                                <th style="width: 100px;" class="noExl">Details</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>



        </div>
    </div>
</div>
<!-- LIST OF CLIENT END-->
<script src="~/Scripts/js/jquery-2.1.1.js"></script>
<script src="~/Scripts/js/jquery.dataTables.js"></script>
<link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
<script src="~/Scripts/js/oms.min.js"></script>

<script type="text/javascript">

    var tempMsg = '@TempData["Message"]';
    var tempFlag = '@TempData["error"]';

    if (tempFlag != "True") {
        if (tempMsg != "")
            toastr.success(tempMsg);
    }
    else {
        if (tempMsg != "")
            toastr.error(tempMsg);
    }
    $("#loading").hide();
    var oTable;

    $(document).ready(function () {
	debugger;

        $("#loading").show();
        var jquey = jQuery.noConflict();
        oTable = $('#myDataTable').DataTable({

            responsive: true,
            "dom": 'lfrtip',

            "bPaginate": true,
            "bServerSide": true,

            "bSortable": true,
            "sPaginationType": "full_numbers",
            "oLanguage": {
                "sProcessing": "<div class=\"loading\" id=\"loading\" style=\"display: block;\"></div>"
            },
            "processing": true,

            "sAjaxSource": "GetConsentFormList",
            "fnServerData": function (sSource, aoData, fnCallback) {
                $.getJSON(sSource, aoData, function (json) {
                    fnCallback(json);
                }).fail(function (jqxhr, textStatus, error) {
                    window.location.reload();
                });
            },
            "fnServerParams": function (aoData) {
                aoData.push({ "name": "FilterOfficeId", "value": $('#ddFilterOffice option:selected').val() });
            },
            "fnDrawCallback": function (oSettings) {
                //Navigation validation
                var rowsPerPage = this.fnSettings()._iDisplayLength;
                var rowsToShow = this.fnSettings().fnRecordsDisplay();
                var table = $('#myDataTable').DataTable();
                var info = table.page.info();
                var currentPageNo = info.page + 1;
                $("#loading").hide();
            },
            "responsive": {
                "details": {
                    "type": 'column'
                }
            },
            "aoColumns": [

                            {
                                "sName": "PatientName",
                                "mRender": function (oObj) {
                                    return '<span>' + oObj["PatientName"] + '</span>';
                                }
                            },
                             {
                                 "sName": "CaregiverName",
                                 "mRender": function (oObj) {
                                     return '<span>' + oObj["CaregiverName"] + '</span>';
                                 }
                             },
                            {
                                "sName": "OfficeName",
                                "mRender": function (oObj) {
                                    return oObj["OfficeName"];
                                }
                            },
                            {
                                "sName": "MRNNumber",
                                "mRender": function (oObj) {
                                    return oObj["MRNNumber"];
                                }
                            },
                            {
                                "sName": "InsertedDateTime",
                                "bSortable": false,
                                "mRender": function (oObj) {
                                    return oObj["InsertedDateTime"];
                                }
                            },
                             {
                                 "sName": "AdmitDate",
                                 "bSortable": false,
                                 "mRender": function (oObj) {
                                     return oObj["AdmitDate"];
                                 }
                             },
                              //{
                              //    "sName": "OfficeName",
                              //    "bSortable": false,
                              //    "mRender": function (oObj) {
                              //        return oObj["OfficeName"];
                              //    }
                              //},
                            {
                                "sName": "AdmissionId",
                                "bSortable": false,
                                "mRender": function (oObj) {
                                    var actionhtml = '';
                                    //actionhtml += '<a href="javascript:;" class="btn btn-xs btn-primary noExl" style="margin-top:5px; width:100%;" onclick="EditClientDetail(\'' + oObj["AdmissionId"] + '\');">View Consent Form</a>'
                                    
                                    //actionhtml += '<a href=' + oObj["SubmittedPdf"] + ' class="btn btn-xs btn-primary noExl" target="_blank" style="margin-top:5px; width:100%;">View Consent Form</a>'

                                    actionhtml += '<a href=' + oObj["SubmittedPdf"] + '   target="_blank" style="margin-top:5px; width:100%;"><img src="http://pasevauat.com/image/pdf.png" style="width:42px;height:42px;"></a>'
                                    return actionhtml;
                                }
                            },


            ]
        });

        $('#myDataTable_filter input').unbind(); //Unbind direct search on each keypress
        $('#myDataTable_filter input').bind('keyup', function (e) { //Bind Keyup event that filter data only on "Enter" key.
            if (e.keyCode == 13) {
                oTable.search(this.value).draw();
            }
        });
        /* Init DataTables */
        $('.carousel').carousel({
            interval: false
        });

        var config = {
            '.chosen-select': {},
            '.chosen-select-deselect': { allow_single_deselect: true },
            '.chosen-select-no-single': { disable_search_threshold: 10 },
            '.chosen-select-no-results': { no_results_text: 'Oops, nothing found!' },
            '.chosen-select-width': { width: "95%" }
        }
        for (var selector in config) {
            $(selector).chosen(config[selector]);
        }

        $("#loading").hide();



    });

    function OpenAddNew() {
     //   $("#loading").show();
        $('#AddNewClient').load(INITURL + 'Client/AddClient', function () {
            sessionStorage.setItem("PartialView", "1");
            $('#myModal').modal({ backdrop: 'static', keyboard: false })
            $('#myModal').modal('show');
        });
    }
    
   
    function Filtration() {

        oTable.search($('#myDataTable').val()).draw();
    }
</script>
